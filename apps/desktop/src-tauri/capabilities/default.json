{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Minimal permissions for the Rynne presentation layer.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
